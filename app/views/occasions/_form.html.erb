<%= form_for @occasion do |f| %>
  <%= f.label :name, class: "label" %>
  <%= f.text_field :name, class: "input" %>
  <%= f.label :date, class: "label" %>
  <%= f.date_select :date, class: "select" %>
  <%= f.submit class: "button is-primary"%>
<% end %>